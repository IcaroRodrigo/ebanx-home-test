spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./data/ebanx
    username: root
    password: 123456
  jpa:
    #database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
   # defer-datasouce-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console